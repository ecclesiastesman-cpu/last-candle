# Чек-лист проверки (перед публикацией)

Локально: `cd game && python3 -m http.server 8080` → http://localhost:8080

- [ ] титул → выбор класса → лагерь → врата акта 1 → бой → лут → босс → лагерь
- [ ] все ассеты грузятся (нет 404 в консоли)
- [ ] 60 fps на сцене с ордой (?dev=1), частицы ≤300, сущности спят вне экрана
- [ ] тач-онли на вьюпорте 390×844 (джойстик слева, скиллы справа, орбы читаются)
- [ ] клавиатура: WASD/стрелки + Space/J атака + K,L,U,I скиллы + Q зелье + Tab инвентарь (физические коды)
- [ ] смерть → потеря золота → возрождение в лагере
- [ ] сохранение: перезагрузка страницы возвращает героя (localStorage)
- [ ] sw.js: после первой загрузки по https игра открывается оффлайн
- [ ] строки только из strings.js
- [ ] относительные пути (работа из подпапки)
